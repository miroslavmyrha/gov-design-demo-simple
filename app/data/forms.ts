export const FIELD_RULE_KEYS: Record<string, string[]> = {
  name: ['rule1', 'rule2', 'rule3'],
  email: ['rule1', 'rule2'],
  note: ['rule1', 'rule2'],
  gender: ['rule1'],
  agreeTerms: ['rule1'],
}

export const VALIDATION_FIELDS = Object.keys(FIELD_RULE_KEYS)
